# Count number of pets in the API response

I have used axios to make the API call.

In the project directory:

run below command to install proeject dependencies:

### `npm install`

run below command to execute the program:

### `node pets.js`
